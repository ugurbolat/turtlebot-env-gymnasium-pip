from setuptools import setup

setup(
    name="qturtle",
    version="0.0.1",
    #install_requires=["gymnasium==0.29.1"],
    # TODO add other dependencies or remove this line since requirements.txt is used
)
